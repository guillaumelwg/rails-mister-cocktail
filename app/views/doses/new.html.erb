<h1>Add a dose to <%= Cocktail.find(params[:cocktail_id]).name %></h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.submit 'Add a dose' %>
<% end %>

